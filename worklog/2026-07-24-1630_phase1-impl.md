# Worklog — 2026-07-24 16:30 — phase1-impl

**Session/agent:** Claude Code
**Phase / directory worked on:** experiments/01_single_kernel_size/
**State:** in-progress (code complete, unrun)

## Summary
Implemented the Phase 1 benchmark (source, build/run scripts, plotting, findings
derivation) per `prompts/01_single_kernel_size.md`. Could not build or execute it —
this session's environment is a Windows machine with no CUDA toolkit / Jetson device.

## What I did
- Wrote the CUDA benchmark: kernel, size x reuse x blocks sweep, correctness check,
  self-reported env fields, CSV writer.
- Wrote `build.sh` / `run.sh` (locks `nvpmodel`/`jetson_clocks`, appends `shared/env.md`,
  both resolve paths relative to themselves so they run from any cwd).
- Wrote `plot.py` (the two required plots) and `derive_findings.py` (computes
  `findings.json` + `FINDINGS.md` from the real CSV — no hand-typed numbers).
- Syntax-checked `.cu`/`.py` files with `py_compile` only (no nvcc available here).

## Files created / modified
- `experiments/01_single_kernel_size/src/phase1_bench.cu` — kernel + full sweep + CSV
- `experiments/01_single_kernel_size/scripts/{build.sh,run.sh,plot.py,derive_findings.py}`
- `experiments/01_single_kernel_size/README.md` — build/run instructions, status, design notes

## Key decisions & rationale
- Did **not** hand-author `findings.json` / `FINDINGS.md` / `shared/env.md` with
  placeholder numbers — conventions §2 forbids "silently substitute a guess", and
  fabricated-looking numbers in `findings.json` would be worse than its absence for
  a downstream phase. Instead `derive_findings.py` computes every value from the
  measured CSV; these three files simply don't exist yet.
- Tier-boundary detection (L2 / L2+SLC / DRAM-bound cutoffs) is a heuristic over the
  highest-reuse plateau curve (biggest single BW drop = L2->SLC step, convergence to
  within 10% of DRAM peak = DRAM-bound). Documented in README "Design notes"; sanity-
  check its output against the plot before trusting it.
- `saturated` flag = within 2% of that (size, reuse) group's max achieved GB/s.

## Results / findings produced
None yet — no hardware run. Nothing to report numerically.

## Upstream I consumed
None (Phase 1 has no upstream dependency).

## Open questions / blockers
- **Blocker:** needs to be built (`nvcc -arch=sm_87`) and run on the actual Jetson AGX
  Orin. Correctness, saturation validity, and the two tier-sanity checks (prompt
  "Verification" section) are all unverified until then.
- Noticed mid-session that another session had concurrently added `worklog/` and the
  mandatory-worklog rule to `prompts/00_conventions.md`, plus `prompts/02_two_kernel_size.md`,
  `03_green_context.md`, `04_zero_copy.md` — none of that was read or acted on beyond
  adopting the worklog convention for this entry (out of scope for a Phase 1 session
  per conventions §5 scope guards).

## Handoff → next session
1. On the Jetson device: `experiments/01_single_kernel_size/scripts/run.sh`, then
   `plot.py`, then `derive_findings.py` (see phase README "Running").
2. Manually verify the prompt's Verification section (tier sanity a/b in particular —
   the script can't judge "does this look right" the way a human glancing at the plot can).
3. Only after `findings.json` exists and looks sane should Phase 2 begin.
